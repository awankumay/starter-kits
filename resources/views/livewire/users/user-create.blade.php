{{-- <div>
    <div class="space-y-6">
        <div>
            <flux:heading size="xl" level="1">{{ __('Create User') }}</flux:heading>
            <flux:subheading size="lg" class="mb-6">{{ __('Add a new user to the system') }}</flux:subheading>
            <flux:separator variant="subtle" />
        </div>

        <div class="bg-white dark:bg-zinc-800/40 rounded-lg p-6">
            <form class="space-y-6">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <flux:input label="First Name" placeholder="Enter first name" required />
                    <flux:input label="Last Name" placeholder="Enter last name" required />
                </div>

                <flux:input label="Email Address" type="email" placeholder="user@example.com" required />

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <flux:select label="Role" required>
                        <option value="">Select role</option>
                        <option value="admin">Administrator</option>
                        <option value="finance">Finance</option>
                        <option value="supervisor">Supervisor</option>
                        <option value="user">Regular User</option>
                    </flux:select>

                    <flux:select label="Status" required>
                        <option value="">Select status</option>
                        <option value="active">Active</option>
                        <option value="inactive">Inactive</option>
                    </flux:select>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <flux:input label="Password" type="password" placeholder="Create password" required />
                    <flux:input label="Confirm Password" type="password" placeholder="Confirm password" required />
                </div>

                <div class="flex justify-end space-x-3 pt-4">
                    <flux:button variant="secondary">Cancel</flux:button>
                    <flux:button type="submit" variant="primary">Create User</flux:button>
                </div>
            </form>
        </div>
    </div>
</div> --}}
